To control output of the serialized XML, you may configure XMT to use a custom XStream instance like below:

```
VersionedDocument.xstream = myCustomizedXStreamInstance;
// XMT serialize/deserialize code follows
```
